#ifndef HEADER_H
#define HEADER_H

#include <stdio.h>      // For printf, perror, snprintf
#include <stdlib.h>     // For exit, malloc, free
#include <string.h>     // For string manipulation functions like strcpy, strcat, strcmp, strrchr, strlen
#include <dirent.h>     // For directory operations like opendir, readdir, closedir
#include <sys/stat.h>   // For file status and directory creation, includes stat, mkdir
#include <limits.h>     // For PATH_MAX, defines the maximum length of a path
#include <unistd.h>     // For rename, used to move files


#endif